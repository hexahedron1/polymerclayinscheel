// hydrocarbon language
// import
=> std
// variable
C qwertyuiop <- 90834
// function
He func + Si arg1 -> 
	std.print + arg1
;
// with return
C func2 ->
	<== C.max
;
// calling
func + "scheel"

C thing <- func2

// custom types

@Ct ->
	// constructor
	Ct ->	
	;
	// with arguments
	Ct + arg1 ->
	;
	H val // field
	// object function
	H !staticVal <- false// static function
	He doStuff -> 
		std.print + val
	;
	// static function
	He $staticFunc -> 
		std.print + "qwertyuiop"
	;
;
// ARRAYS
C₅ <- [ 1, 2, 3, 4, 5 ]